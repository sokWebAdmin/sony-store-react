// components
import LayerPopup from '../common/LayerPopup';

const EstimateSheet = ({ close }) => {
  return (
    <LayerPopup onClose={close}>
      <h1>견적서서서서서</h1>
    </LayerPopup>
  );
};

export default EstimateSheet;